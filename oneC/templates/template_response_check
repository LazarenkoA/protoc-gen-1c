Функция ПроверитьОтвет_{{.HandlerName}}(Ответ)
    Если Ответ.КодСостояния = 200 Тогда
		Возврат Ответ;
	КонецЕсли;

	{{- range $key, $value := .RespCodes }}
    Если Ответ.КодСостояния = {{$key}} Тогда
        Возврат Ответ;
    КонецЕсли;
    {{- end }}

	ЗаписьЖурналаРегистрации("http сервис ""{{.ServiceName}}""", УровеньЖурналаРегистрации.Предупреждение,,, СтрШаблон("Используется не задокументированный КодСостояния (""%1"")", Ответ.КодСостояния));
	Возврат Ответ;
КонецФункции

