Функция Обработчик_{{.ServiceName}}{{.HandlerName}}(Запрос)
	Если ПроверитьПараметры_{{.HandlerName}}(Запрос) <> Истина Тогда
		Возврат СформироватьОтветСервисаREST(400, "Ошибка параметров запроса! Структура запроса не соответствует контракту", Истина);
	КонецЕсли;

	Возврат {{.ServiceName}}Переопределяемый.{{.ServiceName}}{{.HandlerName}}(Запрос);
КонецФункции
